# Pilotys repair OwnerBottomNav duplicate imports v1

Cleans up duplicate imports from:

```ts
@/components/owner/OwnerBottomNav
```

Works from either repo root or `apps/cleaner-web`.

Apply:

```bash
bash pilotys-repair-owner-bottom-nav-duplicate-imports-v1/scripts/install-repair-owner-bottom-nav-duplicate-imports-v1.sh
cd apps/cleaner-web
npm run build
```
