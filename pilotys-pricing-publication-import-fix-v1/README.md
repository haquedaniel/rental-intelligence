# Pilotys publication import fix v1

Fixes the package import error caused by the legacy `from .publisher import publish` statement.

Install from the repository root:

```bash
bash pilotys-pricing-publication-import-fix-v1/install.sh
```

Then rebuild the cockpit container and rerun the dry-run publisher.
