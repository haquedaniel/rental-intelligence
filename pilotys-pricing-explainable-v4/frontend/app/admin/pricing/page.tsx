import {getSupabaseAdmin} from "@/lib/supabaseAdmin";import PricingDashboard from "@/components/pricing/PricingDashboard";import {saveAndRecalculateAdminPricing,saveAdminSeason,deleteAdminSeason,rollbackAdminPricing} from "./actions";export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{property?:string}>}){const q=await searchParams,db=getSupabaseAdmin();const properties=(await db.from("properties").select("id,name,status").order("name")).data||[];const propertyId=properties.some(x=>x.id===q.property)?q.property:properties[0]?.id;let settings=null,seasons:any[]=[],calendar:any[]=[],actions:any[]=[],versions:any[]=[];if(propertyId){[settings,seasons,calendar,actions,versions]=await Promise.all([(await db.from("pricing_property_settings").select("*").eq("property_id",propertyId).maybeSingle()).data,(await db.from("pricing_seasons").select("*").eq("property_id",propertyId).eq("active",true).order("start_date")).data||[],(await db.from("pricing_daily_prices").select("*").eq("property_id",propertyId).order("date").limit(540)).data||[],(await db.from("pricing_publication_actions").select("*").eq("property_id",propertyId).order("created_at",{ascending:false}).limit(20)).data||[],(await db.from("pricing_configuration_versions").select("*").eq("property_id",propertyId).order("version_number",{ascending:false}).limit(20)).data||[]]);}return <PricingDashboard backHref="/admin" backLabel="Administration" properties={properties} propertyId={propertyId} settings={settings} seasons={seasons} calendar={calendar} actions={actions} versions={versions} saveSettings={saveAndRecalculateAdminPricing} saveSeason={saveAdminSeason} deleteSeason={deleteAdminSeason} rollbackAction={rollbackAdminPricing} propertyHref={id=>`/admin/pricing?property=${encodeURIComponent(id)}`}/>}
