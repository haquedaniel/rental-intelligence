# Pilotys controlled pricing publication v9

Adds:
- `--date YYYY-MM-DD` for a deliberate one-night publication test.
- nearest-arrival-first ordering for the automatic queue.
- an explicit failure when a requested date has no eligible current action.
- prominent Preview / Live / Pause controls in the pricing UI.
- backward-compatible `publish()` imports.

No migration is required.
