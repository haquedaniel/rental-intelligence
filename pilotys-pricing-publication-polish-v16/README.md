# Pilotys pricing publication polish v16

- rounds channel prices to a configurable increment (default €1);
- suppresses changes below a configurable meaningful threshold (default €1);
- preserves exact calculated prices and explanations in Pilotys;
- keeps automatic publication chronological;
- records exact last-run publication counters;
- separates initial calendar takeover from normal daily publication in the UI;
- adds queue filters and shows 50 chronological rows at a time instead of an unexplained 12.
